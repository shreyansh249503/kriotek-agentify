# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── FLUX.1-schnell (image generation) ─────────────────────────────────────
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.33.0
sentencepiece>=0.2.0

# ── Kokoro TTS (Apache 2.0, no Triton, CPU/MPS friendly) ──────────────────
kokoro>=0.9.2

# ── SadTalker deps (replaces SadTalker/requirements.txt — avoids version conflicts) ──
face-alignment>=1.3.5
imageio>=2.28.0
imageio-ffmpeg>=0.4.9
librosa>=0.10.0
resampy>=0.4.2
pydub>=0.25.1
scipy>=1.11.0
kornia>=0.7.0
yacs>=0.1.8
scikit-image>=0.21.0
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
safetensors>=0.4.0
numba>=0.58.0

# ── Shared ─────────────────────────────────────────────────────────────────
torchaudio>=2.1.0
soundfile>=0.12.1
einops>=0.8.0
Pillow>=10.0.0
numpy
